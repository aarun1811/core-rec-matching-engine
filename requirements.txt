# Runtime dependencies for rec-engine — exact pins.
#
# Polars is pinned tight (<0.21) so byte-exact expected_output.csv survives
# across minor Polars releases that occasionally change Decimal/Date CSV
# serialization. Widen only after regenerating fixtures.
#
# Typer's transitive deps (click, rich, etc.) are pinned to the versions
# resolved locally against Python 3.12 to keep server installs reproducible.

polars==0.20.31

typer==0.24.1
click==8.3.2
shellingham==1.5.4

# typer -> rich (help formatting)
rich==15.0.0
markdown-it-py==4.0.0
mdurl==0.1.2
Pygments==2.20.0

# ─── API backend (rec_engine_api) ─────────────────────────────────────────
fastapi==0.119.1
starlette==0.48.0
pydantic==2.13.3
pydantic_core==2.46.3
annotated-types==0.7.0

uvicorn==0.34.3
h11==0.16.0
httptools==0.7.1
python-dotenv==1.2.2
PyYAML==6.0.3
uvloop==0.22.1
watchfiles==1.1.1
websockets==16.0

anyio==4.13.0
idna==3.11
